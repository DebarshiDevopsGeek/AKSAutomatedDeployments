apiVersion: v1
kind: Pod
metadata:
  labels:
    app: nginx
    owner: user
  name: nginxuat
  namespace: uat
spec:
  containers:
  - image: nginx
    name: nginx
    ports:
    - containerPort: 8080
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
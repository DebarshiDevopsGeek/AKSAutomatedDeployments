apiVersion: v1
kind: Service
metadata:
  name: AutomatedAKSDeployments
  namespace: python-sample-namespace
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: AutomatedAKSDeployments
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001